<%# binding.pry %>

<%= form_for :deck_search, method: 'post' do |f| %>

    <%= f.label :admin_approved, "Admin Approved: " %>
    <%= f.select :admin_approved, options_for_select({ "All" => "", "Only Admin Approved" => true, "Only Non-Approved" => false }, @deck_search.admin_approved) %><br>
    
    <%= f.label :level, "Levels: " %>
    <%= f.select :level, options_for_select(["All", "1", "2", "3", "4", "5", "6", "7"]) %><br>

<%# YOU ARE HERE - INCORPORATE USERS %>
    <%#= f.label :level, "People: " %>
    <%#= f.select :level, options_for_select(["All", "1", "2", "3", "4", "5", "6", "7"]) %><br>

    <br>

    <%= f.submit "Search Decks" %>

<% end %>
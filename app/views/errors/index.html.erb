<h2>Sorry, there was an issue with the server request.</h2>

<div>
    <ul>
    <% flash.each do |type, msg| %>
        <li><%= "#{type.capitalize}: #{msg}" %></li>
    <% end %>
    </ul>
</div>

<h3>Click the back arrow to return or a link to go somewhere else.</h3>